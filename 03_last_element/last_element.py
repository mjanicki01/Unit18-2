def last_element(lst):
    return lst[-1]
    
# why does print return an unscriptable object and not return?
    """Return last item in list (None if list is empty.
    
        >>> last_element([1, 2, 3])
        3
        
        >>> last_element([]) is None
        True
    """